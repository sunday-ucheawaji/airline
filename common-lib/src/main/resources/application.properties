# common-lib is a shared LIBRARY, not a runnable application.
# This file is mostly unused. If you need shared default properties,
# name the file something like "common-defaults.properties" and
# have consuming services explicitly import it.
#
# Do NOT set spring.application.name here — each consuming service
# has its own application name.
